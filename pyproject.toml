[project]
name = "genjax-intro"
version = "0.1.0"
description = "Bayesian house price prediction using GenJAX"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jax>=0.4.20",
    "genjax>=0.9.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
]
